<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- Event Rentals product category -->
    <record id="product_category_event_rentals" model="product.category">
        <field name="name">Event Rentals</field>
    </record>

    <!-- Add event fields to the product form -->
    <record id="view_product_template_form_event" model="ir.ui.view">
        <field name="name">elksevent.product.template.form</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page string="Event Rental" name="event_rental"
                      invisible="not x_is_event_product">
                    <group>
                        <field name="x_elks_income_account_id"/>
                    </group>
                </page>
            </xpath>
            <xpath expr="//field[@name='list_price']" position="after">
                <field name="x_is_event_product"/>
            </xpath>
        </field>
    </record>

</odoo>
